name = "my-app"
main = "_worker.js"
compatibility_date = "2024-09-01"

[assets]
directory = "."
binding = "ASSETS"

# Cloudflare Workers AI — used by /api/translate to translate planner
# messages in the CS console (JA -> EN). Free tier ~10K neurons/day.
[ai]
binding = "AI"
