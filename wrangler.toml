name = "my-app"
main = "_worker.js"
compatibility_date = "2024-09-01"

[assets]
directory = "."
binding = "ASSETS"

# Workers AI binding removed — requires account-level Workers AI access.
# Re-add `[ai]\nbinding = "AI"` after enabling it in Cloudflare dashboard.
